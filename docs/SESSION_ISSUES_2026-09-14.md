# Session Issues — 2026-09-14

## Exact physical cost model counts data paths as workers

- **Status**: Resolved
- **Environment/condition**: DP exact physical cost model; a federated program whose `X` and `y` anchors use different files on the same worker endpoints.
- **Observed symptom**: Three physical workers were counted as six when both `host:port/data/features` and `host:port/data/labels` appeared in anchor metadata.
- **Root cause**: `ExactPhysicalCostModel.workerCount` deduplicated the full anchor `workerId`, which includes the data path, rather than the physical `host:port` endpoint.
- **Resolution**: Canonicalize every anchor partition with the existing `FederationUtils.canonicalFederatedWorkerAddress` before deduplication. Ports remain part of the canonical identity.
- **Scope**: No measured-runtime constant, network calibration, cost coefficient, solver limit, or candidate rule was changed. The production default 5% optimizer gap remains unchanged; any gap-zero planning run is experimental configuration outside this patch.
- **Modified files**:
  - `src/main/java/org/apache/sysds/hops/fedplanner/fedCostBased/fedExact/ExactPhysicalCostModel.java`
  - `src/test/java/org/apache/sysds/hops/fedplanner/fedCostBased/fedExact/ExactPhysicalWorkerCountTest.java`
- **Verification**: The focused unit test asserts that two files across the same three endpoints count as three workers and that two ports on one host count as two workers.
- **Decision basis**: Correct the physical cost input identity; do not reduce the planner candidate space or add runtime fallback.
- **Remaining issue**: This correction alone does not prove that the production-default DP-selected LM plan or measured runtime changes; both require a separate native experiment.
- **Potential regression risk**: Malformed anchor addresses could canonicalize to `null`; existing anchor construction requires non-blank identities, and the regression suite detects the known path/port identity cases. A future validation change should reject malformed endpoint metadata at anchor construction.

## LM-CG solver-gap and runtime follow-up

- **Status**: Verified for the bounded LM-CG experiment; general calibration remains open.
- **Environment**: Existing Docker WAN-Mid, 3 workers,50Kx128,8CPU/16GB. All runtime variants use this worker-count fix.
- **Intervention**: Existing per-run `sysds.fedplanner.regional.incremental.relativeGap=0`; production default remains0.05. No cost constants tuned.
- **Observation**: Strict solve reached exact lower=upper2809.0965728ms and removed Y prefetch; default corrected-worker objective2836.8670455ms retained it. Strict Cost-based and AggLocal have identical118 normalized runtime instructions.
- **Measured once per variant**: default Cost-based4.972s, strict Cost-based4.319s, AggLocal4.440s. Output parity passed at7 significant digits. Strict-vs-AggLocal difference is not an optimizer improvement claim because the plans are identical.
- **Validation artifact**: `/home/mchoi/lm-cg-followup-20260914/REPORT.md` and native receipts. One pre-execution network-invalid attempt retained and excluded; retry passed without relaxing validation.
- **Residual risk**: exact tolerance may cost more compilation time on larger workloads; worker-union accounting remains global rather than anchor-specific. Figures and existing published measurements unchanged.

- **Materialization diagnostic**: prefetch514.233ms; dispatch0.300ms, response wait504.074ms, coordinator bind/copy2.116ms. Waiting, not coordinator copy, dominates. Remote execution/codec/transport attribution remains open; no arbitrary cold-start surcharge was added. Diagnostic time excluded from runtime comparison.

## AggLocal local-vector continuation across a branch join

- **Status**: Resolved in planner policy and focused regression tests; native WAN-Mid measurement is tracked separately.
- **Environment/condition**: Heuristic/AggLocal planning after the federated physical-normalization rewrite, inside a named function called from a loop. A row-partitioned LM residual is clipped by an `if/else`, both branches produce coordinator-local `z`, and the post-branch gradient/loss consume `z`.
- **Observed symptom**: Although both `z` definitions had already been proven coordinator-local, the branch-join `TRead z` was not included in the Heuristic local prefix. The normalized gradient therefore reuploaded `z`, ran its transpose remotely, and the loss also reentered FED.
- **Root cause**: Exact CFG forwarding required the physical node kind to be literally `TRANSIENT_READ`/`TRANSIENT_WRITE`. A real compiled transient access classified semantically as `BRANCH_JOIN` (or `LOOP_PHI`) was rejected even though the shared compiled-transient authority explicitly recognizes those phi classifications.
- **Resolution**: Use shared `PlacementAnalysis.isCompiledAcyclicTransientForwardAccess` (delegating physical-access recognition to `isCompiledTransientAccess`) for both ends of Heuristic CFG transient forwarding and for its analysis invariant, while admitting the acyclic `BRANCH_JOIN` classification and excluding cyclic `LOOP_PHI`. This admits only real compiled TRead/TWrite operations while retaining function/recompile context, vector shape, exact reaching-definition, and all-predecessors-local checks.
- **Policy effect**: AggLocal keeps legal scalar/vector continuation in CP across an exact branch join. A later operator still reenters FED through the existing exact relocation/native-continuation contract when a distributed sibling input requires it. `LOOP_PHI` remains outside this new authority because admitting a back edge can make a native FED/LOUT demotion marker appear downstream of itself; an iteration-aware marker contract is required before extending the rule to cyclic phi edges. Existing concrete loop TRead/TWrite paths remain unchanged. Candidate generation, privacy legality, FedAll, DP, and the runtime fallback policy are unchanged.
- **Modified files**:
  - `src/main/java/org/apache/sysds/hops/fedplanner/placement/NeutralPlacementGraphBuilder.java`
  - `src/main/java/org/apache/sysds/hops/fedplanner/placement/PlacementAnalysis.java`
  - `src/test/java/org/apache/sysds/hops/fedplanner/placement/HeuristicLocalContinuationTest.java`
  - `src/test/java/org/apache/sysds/hops/fedplanner/placement/CompiledTransientOperationKindTest.java`
- **Regression coverage**: The positive LM function/loop fixture applies the physical normalization, verifies a `BRANCH_JOIN` value, verifies exact CFG-forward edges from both local branch definitions, and requires transpose/loss continuation to remain CP/LOUT. A negative fixture with one local and one federated reaching definition verifies that one local predecessor cannot authorize the phi as local.
- **Passing validation**: `mvn -q -DskipRat -Dtest=org.apache.sysds.hops.fedplanner.placement.HeuristicLocalContinuationTest test` passes 6/6. `HeuristicNativeContinuationContractTest` passes 2/2 and `PrivateAggregateFourPlannerContractTest` passes 11/11. `mvn -q -DskipRat -Dtest=org.apache.sysds.hops.fedplanner.fedHeuristic.CampaignBG014HeuristicL2SvmLoopLocalityRedTest#demotedXdRemainsLocalAcrossNestedLoopTransientReads,org.apache.sysds.hops.fedplanner.placement.HeuristicLocalContinuationTest test` passes 7/7 and proves the existing loop policy is preserved. A final combined run of `CompiledTransientOperationKindTest`, the six local-continuation tests, and the targeted L2SVM loop regression passes 11/11. The shared branch-safe authority tested by these checks is `PlacementAnalysis.isCompiledAcyclicTransientForwardAccess`.
- **Pre-existing validation failures**: `HeuristicProtectedNestedDemotionTest#sharedProtectedFunctionFormalPreventsInfeasibleActualDemotion` fails with `SAME_PLACEMENT component has no common legal state` against both this change and primary classes extracted from the pinned unmodified baseline JAR. `CampaignBHeuristicPathwiseReentryTest#siblingBranchesConsumeOnlyTheirExactCommonRelocationFacts` and `#mergeWithUniqueReachingDefinitionPublishesOneExactReentry` likewise fail on both detached HEAD and this change (expected2/1 reentries, observed0); therefore no claim is made that the full pathwise-reentry suite passes.
- **Environment-limited check**: `CampaignBHeuristicRealVectorPolicyRedTest` could not be evaluated in the local unit-test process because all three methods attempted live privacy RPCs to absent workers at `localhost:1234/1235` and failed closed. This is reported as a validation gap, not a pass or a regression caused by this change.
- **Compiled overlay**: `/grid/3/cofee-lm-sweep-mchoi-20260914/systemds-policy-build/agglocal-cp-branch-phi-v3-20260914/classes`, with the manifest and per-class SHA-256 list in the parent directory.
- **Final Docker WAN-Mid verification**: Reviewed v3 JAR `2a6425861f5b10e16a9df4905ead15d508b1507ec18f2e377f23d633ca55e183`, unchanged native LM50K×128/T20/3workers. Contemporary original AggLocal18.615s/663RPC; corrected AggLocal8.340s/129RPC; same-JAR DP8.415s/132RPC; FedAll32.919s/1503RPC. One sample each, all model/audit CP parity and network/archive guards passed. Actual instruction CSVs confirm corrected AggLocal uses CP transpose and CP tsmm loss, while gradient contraction stays FED. DP and AggLocal share these hot-path placements; no stable ranking claim follows from the0.075s gap.
- **Evidence**: `/grid/3/cofee-lm-sweep-mchoi-20260914/agglocal-cp-rerun-v3/{measurements.json,placement-comparison.json,validation.json,source-evidence/}`. The first broad LOOP_PHI candidate is retained under `agglocal-cp-rerun/` with `SUPERSEDED.json`; it is not used for final validation. Final JAR modifies only the two production classes and their nested classes; runtime/cost-model classes remain unchanged.
- **Remaining issues / regression risk**: The new proof is branch-join-specific, not a general loop-carried locality solution. Mixed-definition and nested-loop regressions guard accidental over-propagation. Known unrelated baseline test failures above remain unresolved. Existing motivation figure still describes the pre-fix plan; do not use its old AggLocal/Cost-based gap as evidence for corrected planners without re-exporting/re-rendering.

## Shared transient placement alternatives (implementation in progress)

- **Status**: In progress; not yet accepted for native execution or performance claims.
- **Problem**: Coarse cross-block replay requires a singleton distributed placement and loses executable writer alternatives before downstream reads/aggregates are generated. FType-only recovery cannot distinguish physical maps or compatible reaching definitions.
- **Implementation direction**: Extend existing candidate emissions with exact placement realizations, proof dependencies and typed input bindings; preserve all writer-reader compatibility edges; propagate semantic changes through existing closure; carry selected realization identity in canonical receipts and physical hard factors. No cost coefficients or runtime fallback changes.
- **Consumer correction**: Per-consumer candidate objective pruning is not sound once writer/reader proofs couple choices. The existing component search must apply its FED-max/CP-min input preference to compatible complete assignments, followed by existing anchor/physical-emission/canonical preferences, rather than deleting the only compatible lower-ranked candidate in advance.
- **Verification so far**: `PlacementRealizationAuthorityTest` passes in the first focused run. New `ExactTransientRealizationTest` passes one compiled cross-block physical-selection fixture. These are partial checks, not completion evidence.
- **Current failures**: Initial 39-test shared-analysis run reports builder join collection/comparison issues, native-continuity sibling handling and closure/privacy regressions; later rerun still loses LM ROW forwarding and rejects semantic replacement as non-monotone. Existing physical certificate tests also exposed a sorted-set null comparison. All are being repaired, not ignored or recategorized as baseline failures.
- **Artifacts**: `/grid/3/cofee-lm-sweep-mchoi-20260914/transient-physical-tests.log`; fresh Maven reports under `target/surefire-reports` (subsequent runs overwrite these reports).
- **Remaining work**: Ground ordinary native output realizations in actual selected input/direct/relocation authority, close proof/layout/compatibility changes to a fixed point, update indexed relocation scoring for exact realization dependence, complete negative join/privacy and physical receipt tests, then run the Docker-only planner validation sequence.
- **Regression risks**: Cartesian proof growth; loop-proof unrolling; old row-separability/cached-cost assumptions; accidental privacy restoration after replay. Detect using bounded exhaustive compatibility fixtures, closure idempotence/narrowing tests, existing privacy/loop suites and exact-vs-indexed selection checks.
- **Decision basis**: Fix shared legality/representation and consumer authority; do not force Cost-based to win or modify measurements.
- **Proof-growth diagnosis and repair in progress**: Recursive realization references included complete ancestor signatures, preventing loop fixed points and causing a tiny LM compiler fixture to exhaust a 3 GB JVM. References are now shallow `(rule, exact realization key)`. Same-layout derivations are represented as alternative support clauses (OR), with each clause retaining its conjunctive typed bindings (AND). Writer-reader alternatives remain relational rather than a Cartesian product. The physical solver and canonical receipts retain the selected clause; clause identity is never embedded recursively into source references. Builder/native grounding and integrated regression validation remain pending.
- **Clause migration validation**: A coordinated 40-test run compiled the migrated consumers. `PlacementRealizationAuthorityTest` passed 7/7 and `PrivacyMovementCertificationTest` passed 9/9. The same run still failed one native-continuity assertion, two LocalBroadcast checks, and one LM relation-proof check; these remain implementation regressions under active repair. The LM fixture now reached relation validation without the earlier OOM, but that is not a correctness pass.
- **Additional authority safeguards under review**: Native continuity needs a typed worker-pool witness (not a parsed signature) so a proven no-movement source does not incur spurious relocation. All clauses sharing one shallow realization identity must agree on the physical pool/layout. Selection signatures must refer only to the selected emission header/layout/clause rather than copying the entire alternative universe into every receipt, avoiding quadratic signature storage.
- **Physical validation after clause migration**: `mvn -q -DskipRat -Dmaven.compiler.useIncrementalCompilation=false -Dtest=ExactTransientRealizationTest,ExactNativeLocalAnchorFanoutCostTest,ExactPhysicalModelCertificateTest test` ran 13 checks: 12 passed; the seven-workload certificate fixture still failed the shared all-reaching-definition relation validator. Log: `/grid/3/cofee-lm-sweep-mchoi-20260914/transient-physical-tests-v6.log`. A preceding v5 build was invalidated by interrupted overlapping Maven compilation (missing unrelated generated FunctionObject classes); v6 regenerated them. Subsequent source edits require another fresh run.
- **Latest incremental evidence**: v8 compiled and passed native continuity 21/21 plus authority 9/9. Further native safety edits and an additional mixed grounded/ungrounded SCC test await rerun. The pre-outer-fixed-point v9 physical run (same compiled snapshot, `surefire:test`) passed 9/14 and exposed stale relocation keys (three errors) and CFG closure nonconvergence (two errors), confirming that post-action enrichment cannot remain a one-shot phase. The current repair moves action/realization binding, CFG replay, downstream candidate regeneration, and privacy into one semantic fixed point. These regressions are not hidden or treated as performance results.
- **Resume validation (v19–v21, still in progress)**: v19 focused six checks passed two (inlined physical selection and indexed relocation), with four shared-builder/fixture errors. v20 enforced publication fail-closed and reran shared closure; three focused tests still failed (two stale canonical reader-state identities, one lost broadcast aggregate state). A separate compiled-snapshot shared run passed 25/27 (compiled transient 4/4, privacy materialization 4/4, relocation index reuse 3/3, shared privacy 14/16). Logs: `transient-diagnostics-v19.log`, `transient-diagnostics-v20.log`, `transient-shared-v20.log` in `/grid/3/cofee-lm-sweep-mchoi-20260914/`.
- **Publication review fixes underway**: Publication must regenerate relation edges after new exact realizations, reapply privacy after physical regeneration, propagate privacy-induced node deltas to downstream candidates, canonicalize candidate emission states against the final graph, and retain final graph-owned action-key identity. Originally emitted work with no executable state must fail closed rather than disappear. Final state projection must also consider candidate-dependent local-output FED states, not merely FOUT. These are correctness constraints, not cost-policy changes. v21 fresh integration is running; no final success or new experiment result is claimed.
- **2026-09-14 resume / exact refinement proof**: The latest parent physical run reports 45 tests with two `ExactPhysicalModelCertificateTest` errors. The seven-workload error rejects removal of a `REPLACE` candidate keyed by `[PRESENT ROW, ABSENT_LOCAL, ABSENT_LOCAL]` even though producer ordinal 91 is already in the refined set and the rebuilt consumer retains only the all-local tuple. This is a false non-monotone classification if and only if the exact input domains used by the consumer rebuild no longer admit that tuple. The current checker re-derives input availability from projected predecessor nodes instead of the authoritative `exactInputDomains` used by `buildNode`, so those views can diverge. A policy-free helper, `CandidateDomainRefinement`, now proves tuple removal directly against the rebuild domains and only at explicitly refined input positions; malformed cardinality and unrelated-input cases fail closed. `CandidateDomainRefinementTest` covers removed ROW, unchanged ROW, unrelated refined input, and malformed-domain cases. Integration into `closePostCfgPhysicalCandidateDependencies` / `isExactAffectedDescendantRefinement` is the next implementation step after this focused compile/test. The separate inlined-function `PROFILE_ERROR` / `EXACT_PHYSICAL_DOMAIN_EMPTY` failure remains open and is not masked by this proof change. Tests for this resume unit are **NOT_RUN** in WebGPT because Maven/SSH execution belongs to the parent.

## Desktop Commander continuation: factorized function-boundary grounding

- Evidence directory: `/tmp/transient-final-20260914-9DuAs0`; inherited dirty diff and builder were copied before edits. No reset/checkout/commit/push.
- Fresh inherited-source run (`focused-before.log`): 35 tests, 33 pass, 2 errors. LocalBroadcastMatmulCandidateTest now passes 2/2; older 10/11 reports are superseded for that fixture.
- Remaining inherited failures: authorized transformencode recode metadata test loses X0's FED native realization during publication; L2SVM mixed formal/ordinary Y read is incorrectly required to have an ordinary-only transient relation.
- `privacy-trace.log` shows FunOut X0 and TRANSFORMENCODE as unbound native rows, then TRead X0 with no states, then sum(X0) rebuilt with only a local input. The final privacy error is a downstream consequence, not permission to relax privacy.
- Expanded diagnostic run (`diagnostic-01.log`): 6 tests, 2 pass, 1 failure, 3 errors. It additionally exposed a ROW relocation action attached to a BROADCAST oracle input row.
- The protected-intermediate fixture used matmul, whose existing policy correctly labels pred/z PRIVATE_AGGREGATE_TO_PUBLIC. It now uses a non-aggregating X+1 transform for its protected-value assertion; the original LM matmul fixture remains separate.
- Changes under validation: exact relocation materialization/input-type match; typed native lineage for proved pools with dynamic output extents; LogicalBoundaryRealizations built only from existing function-value and encoded-primary edges, including every mixed CFG source.
- Boundary proof alternatives are factorized per source and checked in shared receipt selection and Exact legality factors. They do not fabricate physical Hop inputs, runtime relocation, or output geometry.
- Cost coefficients, planner ranking/policy, privacy/TR/TW legality and runtime fallback have not been edited. The new boundary integration and its regression coverage are still being validated; no completion or performance claim is made here.
- Current expanded regression command/results: `focused-02.command`, `focused-02.log`, `focused-02.exit` in the evidence directory. Results are pending at the time of this entry.

### Continuation checkpoint: core regression green, broader qualification in progress

- `focused-05.log` / `focused-05.exit=0`: fresh sequential Maven, **55/55 PASS** (CandidateDomainRefinement 6, NativeMixedWorkerPoolContinuity 5, NativePlacementContinuity 22, TransientPlacementAlternatives 4, LocalBroadcastMatmulCandidate 2, SharedPrivacyPlacementAnalysisContract 16).
- TWrite now preserves its selected input's exact durable anchor or typed native lineage instead of inventing a new output map and accumulating copied transitive proof clauses. This removed the fixture's >16 support-clause explosion without changing the bound.
- LOCAL provenance with multiple available anchors now comes from the selected exact receipt; derivation and pre-emission validation use the same provenance. A missing receipt still fails closed rather than selecting an arbitrary anchor.
- `exact-boundary-06.log`: 38 tests, 31 pass, 7 errors. Six were compiler-only SharedPlannerFunctionPlanPropagationRedTest fixtures querying nonexistent localhost:8001/8002 worker files; the other was a real L2SVM loop-backedge compatibility omission.
- `exact-07.log`: 13 tests, 12 pass, 1 error. L2SVM passed after provisional loop-pass-through seeding was limited to the first pass. LOGREG exposed CFG source indexing counting a TRead sharing a writer's value-version as an additional writer.
- Other active edits appeared in the shared worktree during validation: exact prior-row projection refinement helper + two tests, provisional loop-seed closure, real-definition-only CFG source indexing, and exact pass-through cycle handling in runtime materialization source resolution. These edits were inspected and preserved, not overwritten or attributed to an invented clean baseline. Source snapshots `source-07.patch` through `source-09.patch` record the transitions.
- The temporary affected-cone expansion was removed: the exact prior-row strict-subset proof now handles stale worklist bookkeeping with the actual `exactInputDomains`.
- `authority-08.log`: 34 tests, 30 pass, 4 errors. Three errors were an invalid synthetic-node polarity in the newly added boundary unit fixture (fixed without altering production validation); one was ALSCG's exact loop-carrier materialization-source cycle, subsequently addressed by the preserved exact pass-through source change.
- SharedPlannerFunctionPlanPropagationRedTest now explicitly registers intended public test-source metadata through the pre-existing hermetic test authority. It does not disable privacy checking or change runtime metadata handling.
- New boundary unit coverage: canonical endpoint aliases, different ports, native/local incompatibility, separate explicit-materialization authority, mixed formal/ordinary all-source coverage, idempotence, selected-source pool mismatch, and missing-source rejection. Current recheck: `authority-09.log`.
- Runtime qualification remains unexecuted at this checkpoint. Planned surface: LM control plus PCA, ALS, ALSCG, KMeans, LOGREG, L2SVM, StepLM, GLM, GNMF, GMM; P1_FULL, P2_PREP; SliceLine ADULT/COVTYPE/KDD98/USCENSUS. Follow LM's WAN-Mid / 3-worker / 8-CPU / 16-GB Docker protocol, fixed native workload inputs, CP output checks, and pre/post network validation in DP -> FedAll -> Heuristic -> Exact order.
- Preflight found that the `cofee-experiment:20260901` tag differs on so007; qualification must pin a common immutable image identity, not assume a tag denotes identical software on every host.

## Codex continuation: current-source publication and proof traversal

- **Status**: In progress; runtime qualification remains gated on fresh planner regression.
- **Evidence**: `/tmp/transient-codex-resume-20260915-G2R0lO`, including inherited diff, full Java source hashes, initial authority log, and thread dump. No other Maven was active at launch and the Java hashes remained unchanged during the initial run.
- **Current-state correction**: The worktree already contains the program-owner-scoped publication assertion and removal of the handoff-listed temporary traces. Later prior evidence exists through `direct-cache-16.log`; `authority-09.log` is not a certificate for current sources.
- **Symptoms**: The fresh certificate class reports three missing exact realization references. The Docker-shaped KMeans compiler fixture repeatedly traverses `WorkerPoolAnchorResolver.resolveCandidateRealization`; its thread dump records the repeated dependency walk. Only this continuation's test JVM was terminated, so the initial aggregate authority run is ABORTED, not a completed suite result.
- **Repair/cleanup plan**: First add a bounded shared-proof-graph regression; replace path enumeration with a per-query visited set while preserving the complete anchor union and cycle behavior. Then isolate stale support-clause publication with the existing failing certificate fixture, repair regeneration without remapping or accepting missing authority, and rerun the authority and broad planner suites sequentially.
- **Decision basis**: Preserve exact realization/CFG/privacy authority and all runtime-supported candidate rules. No runtime fallback, cost tuning, or relaxed transient/recompile placement constraints.
- **Risks and detection**: A globally cached partial traversal could lose cyclic or sibling evidence; use a per-query visited set and grounded/ungrounded cycle tests. Stale-clause repair must preserve valid alternative clauses and regenerate current exact input bindings. Source hashes and dedicated copied test reports guard concurrent edits and stale results.
- **Traversal repair**: `WorkerPoolAnchorResolver` now retains visited exact references for the whole anchor-union query instead of enumerating every support path. The new 13-reference shared-DAG regression failed before the change with 8,191 visits and passes with a bound of 25; grounded and ungrounded cycle fixtures also pass. The existing function-return resolver suite passes 12/12. This changes traversal complexity, not pool compatibility or candidate legality.
- **Publication repair**: A regenerated TRead can replace a layout identity while a merged FED/LOUT output still retains an older direct-input support clause. Publication now retains only OR clauses whose entire AND input-binding set references executable realizations in the current exact domain. Surviving alternatives retain their original layout and authority; expired references are not remapped by equal geometry. The existing outer fixed point regenerates and propagates the resulting changes. The two new clause regressions failed before the repair and pass afterward, including the one-live/one-expired conjunct negative case and idempotence.
- **Fresh focused validation**: `publication-green` exits 0: 30 tests, no failures/errors/skips. This includes `ExactPhysicalModelCertificateTest` 8/8, `TransientPlacementAlternativesTest` 4/4, `CandidateDomainRefinementTest` 6/6, `LogicalBoundaryRealizationsTest` 6/6, the original owner-scoped publication assertion 1/1, and the five new tests. Full authority and broader regressions are still pending at this checkpoint.
- **Docker identity clarification**: so002 and so007 retain different Docker image IDs, but the frozen campaign's exact execution-content fingerprint (Architecture, Os, Created, Config, RootFSLayers) matches `0861f4ff197c868f42abf6478b66505f650325c267caa8be2e82b4b6317c7ff0` on both. No image/tag was changed. All-host content certification plus current JAR/wrapper/stage identity remains required; equality of this fingerprint must not be described as equality of Docker image IDs.
- **Authority closure**: `authority-full` exits 0, 20/20 tests: all six function-body fixtures including the Docker-shaped KMeans case, all six logical-boundary tests, and all eight exact certificates.
- **Broader current-source evidence**: `planner-regression` completes 102 tests with 101 passing and one assertion failure in the already-failing inherited `HeuristicLocalContinuationTest.protectedFederatedVectorDoesNotForceIllegalLocalContinuation`. Both privacy prohibitions pass; the exact receipt comparison differs only in the multiplication's selected support clause (an X-anchored versus Y-anchored relocation). Do not weaken that canonicality assertion. Add an indexed-versus-public exact row-domain regression to distinguish filtering from scoring/tie-break disagreement before applying a minimal correction.

## Projected candidate reachability borrowed a different relocation anchor

- **Status**: Fixed in the canonical reachability predicate; focused Heuristic regression passes. Broader validation is in progress.
- **Environment**: Current dirty tree on so002, Java 17; protected-vector Heuristic fixture with X=4x3 and Y=4x1 on the same endpoint. Evidence root: `/tmp/transient-indexed-fix-kgGwN9YZ`.
- **Symptom and reproduction**: The inherited indexed-domain red test retained public candidate ranks `[11,12,20,21,25,26,34,35,39,40,41,42]` versus indexed `[12,21,26,35,39,40,41,42]`. The existing canonical selected-receipt assertion also failed while its privacy assertions passed. `indexed-diagnostic.log` records the missing rows' exact bindings and projected actions.
- **Cause**: Contrary to the initial indexed-loss hypothesis, the public `receiptReachable` path checked only input position, coarse FType and target placement. It accepted four receipts whose position-0 binding required an X-anchored relocation absent from the projected action universe, borrowing the existence of a different action. The index already used `actionMatchesSelectedCandidate` and rejected those exact unsupported rows. Equal endpoints do not authorize substituting a distinct relocation key.
- **Fix**: Reuse `actionMatchesSelectedCandidate` in the public physical-input action check. No candidate generator, runtime capability, privacy policy, cost coefficient or default tolerance was changed. The temporary test-only diagnostic printing was removed after capture.
- **Files**: `src/main/java/org/apache/sysds/hops/fedplanner/placement/CandidateSelections.java`; `src/test/java/org/apache/sysds/hops/fedplanner/placement/HeuristicLocalContinuationTest.java`.
- **Tests**: `heuristic-green` exits 0, 7 tests, 0 failures/errors/skips. The new indexed/public domain comparison is retained and additionally verifies that each returned relocation binding exists in the projected universe. The original canonicality and privacy assertions remain unchanged. Sequential authority and expanded regression runs are pending.
- **Decision basis**: Repair exact selected-action authority at projection, not a convenience capability guard and not a weaker privacy or layout comparison.
- **Residual work**: Runtime qualification remains NOT_RUN. The inherited `cp_reference.py` is only 28 lines and ends mid-render implementation; the qualification adapter also has no executable entrypoint. Actual structured templates are `P1_FULL.dml` and `P2_PREP.dml`, not `p1.dml`/`p2.dml`.
- **Regression risk**: Legacy callers that supplied a different same-FType action instead of a receipt's exact binding will now fail closed. Detect through indexed/public candidate parity, canonical receipt legality and the broader placement/privacy regression suites. Single-input/formal direct-support branches remain separately governed by their existing authority and are not generalized by this patch.
