# Session issues — 2026-09-06

## GLM partitioned native candidate proof — in progress

- Symptom: backend73533ea authenticated GLM/LAN/w1 three selectors passed; w3/FedAll fails `No privacy-safe physical placement` at `glm.dml:869` PRIVATE_AGGREGATE Binary multiply. Campaign status3/54 complete, fourth cell failed; no new runtime result.
- Evidence: `/home/mchoi/g014-runtime-4net-w1357-20260901-control/GLM_W3_CANARY_FAILED_73533ea_COORDINATOR.log`; its archived coordinator SHA-256 is `08fe8a298284e50d732b1280da922b7e11087b2b587ef5f0854e9db59f8b31f9` (compressed archive, not extracted log hash).
- Root cause verified: `BinaryElemwiseRule.caps` consulted unknown concrete output/input dimensions while exploring other paths before reaching the exact ROW/ROW case. `ShapeHint` records consultations monotonically, so `OracleFacade` exported irrelevant missing shape facts and `NeutralPlacementGraphBuilder` excluded the valid native emission as `UNKNOWN_METADATA`. The common abstract shape at the failed multiply is actually exact 50000 x 2. Privacy correctly rejects the remaining rows because they require one protected local input.
- Repair: recognize exact ROW/ROW and COL/COL native candidates before speculative shape probes; retain the representation guard and downstream exact candidate/worker-range certification. Delete the now-unreachable old soft-axis block. Do not clear ShapeProof globally or alter the privacy filter.
- Modified production file: `src/main/java/org/apache/sysds/hops/fedplanner/rules/Rulesets.java`. Regression files: `OracleFacadeTest.java` and `GlmPrivateAggregatePlanningContractTest.java`.
- Repair plan: `CONTROL/GLM_PARTITIONED_NATIVE_CONTINUITY_REPAIR_20260906.md`.
- Root test change: add 3/5-worker balanced PRIVATE_AGGREGATE fixtures to `GlmPrivateAggregatePlanningContractTest` and exercise the same three additional-ML selectors. Existing w1 method remains available. Test fixtures compile local metadata only, not authenticated runtime.
- Validation: actual GLM w3/w5 is RED against73533ea. Isolated Rulesets-only patch passes all three additional-ML selectors for w1/w3/w5; each selector consumes its own fresh equivalent shared analysis and legal domain. Oracle unit coverage includes unknown-dimension ROW/ROW and COL/COL positive cases and mixed-axis negative shape requirements. Root integration/regression and authenticated Docker recheck remain pending.
- Deferred unrelated change: the earlier NativePlacementContinuity ROW/COL extension passed its micro tests but did not fix the failed pre-domain exclusion. Its diff is preserved at `CONTROL/DEFERRED_NATIVE_ROW_COL_CONTINUITY_20260906.diff`; it was removed from the integration tree. The GLM Rulesets-only pass uses the unchanged baseline continuity implementation.
- Remaining issue: verify exact physical-map compatibility independently, integrate/build, then rerun the failed authenticated cell before resuming the campaign.
- Regression risk: overpermissive range alignment; detect with mismatched shape/range/anchor negative tests. No protected collection/CP reupload/runtime fallback permitted.

## Detached campaign process lifetime — resolved operationally

- Symptom: shell nohup launcher disappeared before writing any campaign event when its tool process tree ended.
- Resolution: preserved prestart log/pid, relaunched in detached tmux `cofee-extra-ml-planning-73533ea`. Verified stable PID and first cell_started event23:38:59UTC, followed by three successful cells.
- Remaining issue: campaign subsequently stopped on the real w3 planner failure above, not on process-lifetime loss.
- Risk: never infer running state from PID file alone; require live process/session and campaign event/receipt evidence.

## Runtime reversal classification — evidence complete, repairs open

- Required order: Exact <= DP <= {FedAll, Heuristic}; no ordering between the two baselines.
- Authenticated archived instruction audit: 74 in-scope pairs = 34 identical full streams + 9 identical after scratch-process normalization + 31 changed streams. The 54 baseline-to-baseline comparisons are excluded. No evidence is missing or hash-invalid.
- Evidence: `CONTROL/PLANNER_REVERSAL_PHYSICAL_PLAN_CLASSIFICATION_20260906.csv` and `.md`. Same-plan does not prove absence of runtime variability, and changed-plan does not by itself prove a cost defect. No pair is marked repaired from this classification.
- Next: concentrate candidate/cost diagnosis on the 31 changed-plan pairs and use repeated matched-plan measurements for the other 43. Exact worker-scaling cases remain a separate 30-case investigation.

## StepLM output-byte estimate collapse — in progress

- Symptom: current Exact assigns equal result-download estimates to dynamic A=t(X)%*%X and b=t(X)%*%y, although b has a proven one-column abstract fact.
- Corrected diagnosis: an occurrence-aware probe shows both generic effective outputs equal 268435456 bytes (256 MiB), raw HOP estimates 32178700288 bytes, and no direct node anchors. The weighted 80747916.68-ms trace term is compatible with that generic unknown-dimension envelope. It is NOT evidence that these particular A/b values were sized from a 50000 x 2100 durable anchor.
- Evidence: `/tmp/steplm-output-probe.log`; historical/current causal artifacts under CONTROL. A helper that treats anchor geometry as exact derived geometry is separately suspect, but changing it alone does not address this reproduced symptom.
- Decision: investigate a shared occurrence-aware partial/exact shape estimate without inventing a 2100 feature bound from a worker-pool anchor. No production cost patch applied yet. DP/Exact objective parity must be tested together.
- Remaining risk: a conservative estimate is not a physical upper bound; report the distinction and validate selected-plan changes before runtime claims.

## GLM integration verification update

- Fresh Maven affected-surface suite: **298/298 PASS**, zero failures/errors/skips, 49 suites; receipt `CONTROL/GLM_SAME_AXIS_WIDE_VERIFIED_20260906T015700Z.json`.
- Fresh actual GLM PRIVATE_AGGREGATE same-analysis selector canaries: worker1/3/5 x FedAll/Heuristic/Exact = **9/9 PASS**; receipt `CONTROL/GLM_SAME_AXIS_SELECTOR_CANARY_20260906T015857Z.json`.
- Added two-worker shifted ROW/COL partition regression to `RelocationSelectionsPhysicalAnchorTest.java`; **6/6 PASS** (four existing plus two new), `CONTROL/GLM_SHIFTED_PARTITION_SAFETY_20260906.log`. Matching endpoints are not sufficient to align partition boundaries.
- Composite receipt: `CONTROL/GLM_SAME_AXIS_INTEGRATED_REGRESSION_20260906.json`. `git diff --check` is clean. Docker canary remains the next gate; these are not runtime performance results.
- Read-only review correction: an empty inferred profile is not `PROFILE_ERROR`; `CandidateProfileFact.available()` checks the evaluation failure string. The exact GLM ROW/ROW fact is AVAILABLE with no profile failure and retains its native emission. No unrelated profile expansion is included.

## GLM Docker promotion — resolved for the failed planning cell

- Committed backend: `6c659929b0f2cf5a04b4cce68900a0ef0d0f14ab`; pinned JAR SHA-256 `cc1224e0dcbd87d419e85dae455f3dc3eff35af9b08ec477c3e93506cbf51128`. Build approval and complete so002–so009 deployment receipts are under CONTROL. so001 is not an execution host.
- The previously failed `GLM/LAN/w3/FedAll` authenticated Docker planning canary passed at 2026-09-06T02:19:23Z: FedPlanner1.623969s, compilation11.337206s, execution0s, FED instructions/UDFs0, output files0. These trace-enabled planning timers are not overhead-free runtime-campaign measurements.
- Evidence: `/home/mchoi/g014-extra-ml-glm-w3-canary-20260906-6c65992/results.jsonl` and archived receipts; subsequent full54 planning root `/home/mchoi/g014-extra-ml-planning-20260906-6c65992-seeded`.
- Remaining: full54 planning and actual runtime/numeric parity. The conditional runtime continuation authenticates all54 planning archives before launching, uses the same immutable stage and shared lane, and preserves failed-run evidence. Root28/28 Python tests and independent4/4 focused tests pass. Runtime has not started at this update.
- Regression risk: successful planning is not proof of runtime feasibility or numerical equality; both are separate gates. Future source changes must not alter the running immutable stage.

## ALS DP selected transient carrier identity — in progress

- Symptom: current DP fails `No valid federated plan ... TRead W` for real ALS maxinneriter10, worker4/5. Exact and Heuristic pass. This is distinct from historical small single-run performance reversals.
- Cause isolated by debugger: exact transient reuse resolves a cloned TWrite to its original for validation, but then passes the original's TRead to a selected-plan identity check that correctly expects the clone's direct input. Clone and original identities are not interchangeable at this boundary.
- Repair plan: retain canonical metadata checks while using the selected carrier's own direct input for exact binding checks. Do not weaken function/copy/privacy guards or select an arbitrary alternate candidate. Regression RED and narrow implementation are in progress in the dedicated agent lane.
- Evidence: CONTROL/ALS_DP_TRANSIENT_CARRIER_REPAIR_20260906; initial inspector `/tmp/als-dp-failure-w5-predicates.log`.
- Remaining: root integration, affected-surface tests, actual planning delta and changed-cell runtime validation.
- Risk: carrier/canonical mixing can also affect function-copy bindings; require exact sibling/occurrence negatives, not merely a positive ALS canary.

## Native result W2C configuration precedence — in progress

- Symptom: generic codec210 and explicit W2C14.7 yield native FED/LOUT in-band210. A fresh-JVM property probe is RED; a dedicated in-band333 override is already honored.
- Cause: the native-result fallback explicitly prefers generic before directional W2C. Generic210 has C2W upload-hotspot provenance, not a measured native W2C codec benchmark. Native and explicit GET responses use the same Netty Java ObjectEncoder/ObjectDecoder plus MatrixBlock Externalizable serialization; their batching/materialization critical paths can still differ.
- Repair plan: dedicated in-band override > directional W2C > generic fallback, using the existing configuration capture rather than a duplicate helper. Preserve explicit zero/disabled codec semantics and fail-closed privacy/physical candidate domains. Test fresh JVMs to avoid captured-static property contamination.
- Evidence: CONTROL/KMEANS_INBAND_DIRECTIONAL_PRECEDENCE_20260906, isolated probe `/tmp/g014-inband-precedence.wVWmr4/receipt.log`.
- Remaining: integrate tests, compare DP/Exact selected plans, measure authenticated runtime; the absolute correctness of14.7 remains a calibration question, and the fallback change alone is not evidence that KMeans ordering is repaired.
- Risk: changing a shared estimated transfer rate can affect many cost-selected plans; immutable-stage versioning and changed-plan-only replay are required. Do not retune the constant solely to enforce an observed ranking.

## ALS carrier and native W2C repair integration — source verified; runtime pending

- Changes: `FederatedPlannerDpCostEnumerator.java` now supplies the selected TWrite carrier's direct input to the two exact binding predicates; canonical inputs remain in the logical-source and cycle guards. `FederatedCostModel.java` now obtains its in-band default from the existing directional W2C setting, which already inherits the generic default. No new helper, candidate exclusion, privacy relaxation, or runtime fallback was added.
- Regression evidence: the real worker-5 ALS test is RED before the carrier fix and GREEN afterward; worker-4/5 metadata-only planning also succeeds. Fresh Maven runs the new ALS test **1/1 PASS** and the complete cost fallback suite **47/47 PASS**, including four fresh-JVM configuration-precedence cases (generic, directional, dedicated, and explicit zero).
- Wider validation: `CONTROL/DP_CARRIER_W2C_INTEGRATED_20260906T023214Z.json` records **380 tests: 366 passed, 9 failures, 2 errors, 3 skips**. Every one of the eleven failing/error methods reproduces with the same semantic cause against the independently pinned pre-fix 6c JAR. This establishes no introduced failure in the affected-surface run; it does **not** make the wider suite green. Source hashes did not change during validation; independent bounded code review and `git diff --check` pass.
- Baseline attribution: `CONTROL/ALS_PINNED_6C_SIX_FAILURE_CLASSES_20260906.log` (SHA-256 `59301ac559d0f627ce362bf44a6b3430b55f4a856e3a3454fedde856326650cf`) and `CONTROL/ALS_DP_TRANSIENT_CARRIER_REPAIR_20260906.md` (SHA-256 `23e96eab029860257d3ae1582884bf11262fd60e00aaf61fe692cd6b40930c67`). Existing unsupported receipt assertions are separate from the real StepLM local-conflict and LogReg REXPAND failures. The three pre-existing public-fixture skips are retained, not expanded.
- Remaining: isolate the pre-existing StepLM failure, authenticate a new immutable backend, compare physical plans against the existing runtime evidence, and rerun changed cells only. Running GLM/GNMF/GMM-VVI stages remain on their independently pinned 6c backend; this working-tree repair must not replace their JAR mid-campaign.
- Calibration caveat: an isolated pinned-backend Docker codec diagnostic on so009 measured about 404–493 MiB/s median encode+decode throughput for the 10000 x 50 result block. Therefore the configured 14.7 MB/s must not be described as a measured pure native codec rate. The precedence repair fixes the configuration contract; actual transport/binding and matched workload runtime still require separate measurement. Evidence: `CONTROL/KMEANS_FEDERATED_RESPONSE_CODEC_BENCHMARK_20260906`.
- Regression risks: carrier-vs-canonical identity mistakes remain guarded by exact edges, occurrence identity, state/layout agreement, and grounded logical sources. Absolute transfer-rate accuracy remains open; runtime ordering is not an acceptance shortcut for tuning constants.

## Single-worker private LogReg REXPAND candidate omission — source verified, runtime replay pending

- **Symptom**: the actual one-worker private-aggregate LogReg program fails shared planning at its one-hot `REXPAND` even though the FED instruction supports the input. This is a deterministic candidate-space omission, separate from runtime ordering and stale legacy DP receipt assertions.
- **Cause**: `Rulesets.RexpandRule.caps` accepted only exact ROW. Runtime `ParameterizedBuiltinFEDInstruction` uses `isFederated(ROW)`, whose runtime type predicate includes FULL, and preserves FULL when copying/transposing the single-entry FederationMap. `SinglePartitionFacts` did not propagate that conditional single-endpoint fact through REXPAND.
- **Repair**: add native FED/FOUT/FULL only with an explicit single-partition proof; retain the representation guard and BROADCAST/unknown-or-multi-partition rejection. Propagate endpoint cardinality, not exact output dimensions or stale seed ranges. No privacy weakening, transient CP/FOUT, candidate pruning workaround, or runtime fallback.
- **Production files**: `rules/Rulesets.java`; `placement/SinglePartitionFacts.java`.
- **Regression files**: `RexpandRuleTest`, `SinglePartitionFactsTest`, `PrivateAggregateFourPlannerContractTest`, `RexpandFEDInstructionFullNativeTest`.
- **RED/GREEN**: two semantic failures before repair (FULL native emission; single-endpoint propagation), followed by 28/28 focused tests. Runtime tests already pass against the unchanged kernel: both directions use the real in-process worker/CP kernel, preserve correct numeric values/FULL ranges, and issue one EXEC_INST without GET/PUT during expansion. Test-only output inspection occurs after that assertion and is not a privacy-release claim.
- **Shared-domain validation**: fresh seven-suite run **81/81 PASS**, zero failures/errors/skips; source hashes unchanged during the run. Four production selectors consume the same privacy-filtered analysis fingerprint; one-hot protected states are FED/FOUT/FULL, with no local materialization or relocation. Fixture uses the production static `outer`-to-REXPAND rewrite; bare `sum(table(...))` was optimized away, and table-to-REXPAND depends on shape facts unavailable before metadata acquisition. Each rewritten fixture resets the previous planner run state and cleanup restores it.
- **Actual workload validation**: four fresh-JVM production CLI runs for real `multiLogReg` (50000x2100, one worker, PRIVATE_AGGREGATE X/Y, maxi30/maxii5) all reach HOP/LOP/instruction construction and commit 486 legal decisions, including one protected native FULL expansion. Production DP is `DP-LocalConflict`, not the legacy memo-DP receipt. All four report execution0, no model output, and zero fallback/repair. These local compile-only checks are **not Docker performance results**.
- **Evidence**: `CONTROL/LOGREG_FULL_REXPAND_SHARED_DOMAIN_WIDE_20260906.json`, its copied XML directory, `CONTROL/LOGREG_FULL_REXPAND_PRODUCTION_CLI_20260906/receipt.json`, and RED logs.
- **Remaining**: independent source review/commit, versioned immutable deployment, old/new physical-plan comparison, changed-cell-only Docker runtime. The older `CampaignBG014DpLogRegTransientForwardRedTest` now reaches its outdated `DpInvocationReceipt` assertion; its failure is preserved, not masked to claim a green wider suite.
- **Regression risks**: an unproved FULL or multi-endpoint map must not be admitted; negative caps/cardinality tests cover both. Cardinality propagation must not become exact dimension/range authority; tests explicitly reject fabricated geometry.

## Forwarded SSH agent stall — operationally recovered

- **Symptom**: normal SSH to so002/007/008/009 stalled before offering a public key; `ssh-add -l` itself exceeded five seconds. Remote banner/key exchange worked. No failed planner result can be inferred from this authentication stall.
- **Cause**: inherited `SSH_AUTH_SOCK` references a nonresponsive VSCode-forwarded agent. Bypassing only this lookup authenticates with the existing on-host key; there is no new credential or authentication bypass.
- **Repair**: run detached experiment supervisors with `env -u SSH_AUTH_SOCK`, preserving normal host-key verification/BatchMode. Verified so003/006/007/008/009; no global SSH configuration, server restart, host qdisc modification, or user-agent termination.
- **Evidence**: `CONTROL/SSH_AGENT_STALL_RECOVERY_20260906/receipt.json` and successful no-agent host probes. The earlier GNMF coordinator SSH255 is preserved separately; the same root cause for that earlier disconnect is plausible but not proven.
- **Remaining/risk**: fresh recovery root imports only the23 authenticated successes and retries failed+remaining31. Original failed artifacts must never be overwritten. The resumed process must not inherit a renewed VSCode agent socket from tmux.

## StepLM nested CFG worker-pool proof — regression reproduced; entry distinction verified

- **Status**: repair in progress, not yet a production/runtime closure.
- **Fresh failure**: `WorkerPoolAnchorResolverCycleTest` models the actual nested identity-write topology: the line-163 consumer read has exactly one immediate CFG definition; the seed lies deeper behind an identity write and header read. On committed `fc30d9`, this expected grounded FULL pool returns empty. Pure no-seed and conflicting selectable pool-B cases remain correctly empty.
- **Original workload fixture**: fresh root Maven also reproduces `LOCAL_CONFLICT_BLOCK_INFEASIBLE` from `CampaignBG014DpStepLmFunctionInputCandidateRedTest` during direct `constructLops`; its later legacy memo-DP receipt assertion is a separate stale test contract.
- **New counterevidence**: a fresh production CLI (`DMLScript.executeScript`, `benchmark.compile_only=true`) on the same committed backend successfully compiles PRIVATE_AGGREGATE StepLM50000x2100, worker1, maxi20 through LOP/instruction construction: 481 legal decisions, protected line-163 X_global FOUT, no fallback/repair/runtime output. Therefore the direct-constructLops failure must **not** be reported as an unconditional production CLI or authenticated runtime failure. The precise entry/metadata difference is under independent analysis.
- **Root-cause refinement**: the archived sibling-only fallback never reached native continuity for the actual wrapper: its sole immediate source re-enters the active SCC, leaves `common == null`, and returns. The earlier positive synthetic test put the seed immediately on the queried read, missing this boundary.
- **Repair boundary**: preserve the acyclic resolver and all CFG/function conjunctions; for an unresolved explicit CFG source, prove a finite existing anchor from the original producer through the complete dependency SCC. Keep every selectable exact native row in the proof. A convenient alternative row, unrelated anchored branch, or ungrounded cycle cannot certify the whole state. The initial FULL-append BROADCAST auxiliary assumption was rejected by runtime review and two fresh hostile regressions. PRESENT BROADCAST is a real federated input, not the instruction-local ABSENT_LOCAL case: aligned append may retain the left BROADCAST map, while map-bind may combine worker pools. State-only native FULL continuity cannot assume it preserves the requested pool without row-conditioned physical authority.
- **Evidence**: `CONTROL/STEPLM_NESTED_CFG_ROOT_RED_20260906.json` (4 tests: 2 pass, 1 semantic failure, 1 actual fixture error); `CONTROL/STEPLM_CFG_PRODUCTION_CLI_20260906/BASELINE_fc30_RESULT.json` (production CLI PASS). The CLI log filename contains `red` because that was the expectation before execution; the receipt records the actual PASS. An initial diagnostic-class compilation typo is separately preserved and is not a planner failure.
- **Modified files planned**: `NeutralPlacementGraphBuilder.WorkerPoolAnchorResolver`, narrow `NativePlacementContinuity` dependency handling, focused cycle/continuity tests.
- **Remaining**: regression GREEN, direct-constructLops progress beyond infeasibility, production CLI nonregression, independent review, exact/privacy/physical-anchor regression surface, then immutable changed-plan comparison. No runtime performance claim is made.
- **Regression risk**: globally certifying one candidate row's pool would create unsound direct-placement authority for other rows. The different-pool selectable-row negative and existing function/pure-cycle negatives must remain passing. No TRead/TWrite CP/FOUT, protected collection or runtime fallback is introduced.

## Isolated verification and storage preservation

- **Status**: operational safeguards active. Additional ML runtime started at 03:48:37 UTC on the pinned 6c stage; workers include the local so002 host.
- **Repair**: all subsequent heavy Java/Maven verification moved to so008, using a source-hash-verified workspace and workspace-local Maven repository. No so001 experiment use, active stage change, qdisc change or shared worker restart.
- **Evidence**: `CONTROL/SO008_VERIFICATION_WORKSPACE_20260906/RECEIPT.md`; source overlays are checked by local/remote full-file SHA256 manifests before testing. `final_sync_v2.sh` consistently excludes .git/target/.omx on both sides, preserving the original helper.
- **Storage**: four inactive build-output trees were copied to so008, all regular-file paths/sizes/SHA256s compared, verified unused and unchanged, and only then removed locally. Remote post-removal hashes were verified again. Free space increased from about 1.20 to 2.75 GB; active experiment evidence and current build outputs were retained. Receipt: `CONTROL/DISK_CAPACITY_MOVE_20260906/RECEIPT.md`.
- **Remaining / risk**: early runtime cells overlapped short local verification before this safeguard; any timing effect is unquantified, not silently dismissed. These measurements must retain provenance and can only be repeated as separately labelled diagnostics, never overwritten to force an ordering.

## StepLM postprivacy stale input row — decisive diagnosis and rejected unsound repair

- **Status**: in progress; v6 affected-surface tests are 123 PASS/1 ERROR out of124, not a passing integration.
- **Symptom**: actual HYBRID StepLM's protected cbind loop has an available `[PRESENT FULL, PRESENT BROADCAST]` candidate input row, but the exact RHS rix's final privacy-filtered domain contains only native `FED/FOUT/FULL`.
- **Cause**: input combinations enumerated before privacy filtering can remain as candidate facts after a producer loses its BROADCAST materialization. The universal native-pool proof treated that row as selectable and failed. Diagnostic instrumentation was confined to an isolated so008 verification workspace, not committed or put in experiment stages.
- **Rejected repair**: missing a direct producer `FOUT/BROADCAST` state is not sufficient by itself. A public/released producer can still satisfy the input with an explicit sibling-anchor relocation; a same-ValueVersion emitted alias can provide the materialization. Independent review rejected v7 rather than removing a legitimate row to pass the test.
- **Repair contract**: only exact postprivacy origin-residency authority plus absence of a selectable BROADCAST output on every emitted owner of the source ValueVersion can prove the row inactive. Preprivacy proof instances do not obtain this exclusion authority. Active PRESENT BROADCAST rows remain subject to the normal universal pool proof; they are not silently treated as local auxiliaries.
- **Simplification planned**: remove the experimental specialized FULL-append/derived-BROADCAST shape/action authority introduced before the decisive diagnosis, provided the actual HYBRID fixture remains green. The existing generic conjunctive dependency proof already handles FULL with local inputs and FULL/FULL.
- **Modified files**: `placement/NativePlacementContinuity.java`, `placement/NeutralPlacementGraphBuilder.java`, focused continuity/SCC/actual StepLM contract tests.
- **Evidence**: CONTROL/STEPLM_SO008_REMOTE_VERIFICATION_20260906/v6-guard-diagnostic/javac-junit.log and v6-integrated/receipt.json; independent architect source review of CandidateSelections relocation reachability.
- **Residual / regression risk**: collapsing occurrence and value identity or treating public relocation as forbidden would unsoundly certify a pool. Regressions must include private-inactive positive, public-movement negative, same-value-alias negative, unknown CFG/function sources, ungrounded cycles, and different-pool selectable rows. No new candidate-space closure or runtime fallback is authorized.

## Extra ML GNMF cross-planner output mismatch — diagnosed separately from planning

- **Status**: runtime stopped safely at 04:30:52 UTC,20/54 successful cells. Planning54/54 had passed. GLM18 runtime cells succeeded; GNMF LAN/w1 FedAll and Heuristic succeeded before Exact failed the cross-planner semantic gate.
- **Observed evidence**: Exact produced its model and reported execution6.138s, but its decimal-significant-digits7 semantic hash differs from the identical FedAll/Heuristic hashes. This is not proof by itself of either an algorithmic error or harmless floating-point variation.
- **Action**: preserve all21 result rows, remote archives and the failed token; independently compare raw numeric outputs, initial random state, DML and selected physical plans. Do not weaken the gate, overwrite the failed row or treat the6.138s value as an accepted successful runtime.
- **Evidence**: `/home/mchoi/g014-extra-ml-runtime-20260906-6c65992-seeded-recovery1/results.jsonl`, token `d2d27449ed4d8ed66850`; CONTROL/GNMF_RUNTIME_PARITY_DIAGNOSIS_20260906/.
- **Residual / risk**: the active campaign has no running next cell until this failure is resolved or isolated in a separately authenticated recovery plan. Do not report that runtime is still progressing.

## StepLM native-pool repair — final source/CLI verification passed

- **Status**: source-level correctness verified; changed-plan runtime replay is still pending.
- **Final repair**: grounded SCC fallback from the exact queried occurrence, complete CFG/function source conjunctions, explicit incomplete-source rejection, and exact postprivacy removal of provably unselectable BROADCAST input rows from the native-pool proof quantifier. The experimental active-BROADCAST/shape/action exception was deleted entirely. All active rows still participate universally.
- **Fixture correction**: actual four candidate rows belong to one append occurrence. The initial simplified positive accidentally modeled four distinct append producers, one with only an impossible row; its rejection was correct. The corrected one-occurrence fixture and the real HYBRID contract both pass.
- **Regression**: fresh so008 integrated Maven **176/176 PASS**,16 suites, zero failures/errors/skips. Includes continuity/SCC, actual StepLM, function returns, shared privacy/four-planner contracts, physical anchors, single-partition/REXPAND,47 cost fallbacks, real ALS carrier, and four GNMF-related tests. Receipt: CONTROL/STEPLM_GNMF_FINAL_INTEGRATED_20260906/receipt.json. A later explanatory source comment changes no behavior.
- **Production entry**: four production selectors x SINGLE_NODE/HYBRID **8/8 PASS**;481/501 decisions respectively. Every protected line163 read remains native FULL, all selected states are analysis-owned legal members, workload execution0, output absent, fallback/repair0. Receipt: CONTROL/STEPLM_MINIMAL_PRODUCTION_CLI_20260906/receipt.json.
- **Risk / remaining**: these are correctness tests, not Docker runtime or overhead-free compile measurements. No performance reversal is closed by this result. A clean committed package and fresh immutable stage remain required before changed-cell replay.

## GNMF nonnegative initializer contract — regression RED/GREEN verified

- **Proven bug**: the builtin documents nonnegative X/W/H but initialized W/H in [-0.05,0.05]. The experiment already shifts X by the permitted global min to make X nonnegative, and both explicit seeds agree across planners. All old outputs contain negative factors. Exact has two catastrophically divergent factor rows, max absolute discrepancy7.232e8; this is not a rounding-hash boundary.
- **Repair**: both initializer lower bounds become0, preserving0.05 upper bounds, explicit/unspecified seed semantics, rank, epsilon, equations, iteration count and parity gates. This repairs the factor-domain contract, not a planner heuristic.
- **RED**: frozen6c JAR plus frozen6c builtin with the new contract test fails `W0 ... negative ... -0.023348881970494995` at maxi0. Evidence: CONTROL/GNMF_NONNEGATIVE_RED_GREEN_20260906/frozen-6c-red.log. The test does not depend on planner ordering to fail.
- **GREEN**: new seeded initialization and five updates produce finite/nonnegative factors; explicit/default seed regression and both prior GNMF CP rewrite modes pass in the176-test integration.
- **Files**: scripts/builtin/gnmf.dml; BuiltinGNMFNonnegativeTest.java; GNMFNonnegative.dml.
- **Remaining / risk**: the first divergent archived arithmetic operation is not identified. Kernel-order perturbation amplified by signed updates is a hypothesis, not proven trace causality. Authenticated new Docker parity must still validate the corrected workload. Old GNMF outputs are not reusable because builtin initialization semantics changed even if a physical placement signature remains identical; preserve them as failed/invalid-precondition evidence. No old immutable stage is modified.

## GMM independent continuation — attempted, new first-cell failure preserved

- **Status**: reauthenticated54 planning archives and independently launched only18 previously unattempted GMM cells using the unchanged6c stage. The first FedAll/LAN/w1 cell stopped at04:47:05 UTC with DMLRuntimeException;0/18 successes. The failure does not overwrite the separate20 successful GLM/GNMF cells or GNMF parity failure.
- **Isolation**: new local/remote evidence roots, shared experiment lane lock, pinned old definition/status/results, old stage and controller byte identities. Supervisor preserves runtime_started=true on error (focused regression passes). All experiments remain Docker based; no so001 worker use.
- **Evidence**: CONTROL/GMM_INDEPENDENT_CONTINUATION_20260906; `/home/mchoi/g014-gmm-runtime-20260906-6c65992-independent/results.jsonl`, token fa9f5b7551b29a534c81; CONTROL/GMM_RUNTIME_FAILURE_20260906/.
- **Remaining**: executor independently tracing the actual worker/coordinator exception and selected plan. No scope-wide fallback, privacy weakening or acceptance of a failed cell.

## GMM parallel two-output row-index aggregation — kernel bug reproduced and repaired

- **Proven failure**: authenticated GMM/VVI FedAll/LAN/w1 reaches legitimate `FED uarimax ... 8 2 FOUT`; its worker CP instruction writes a two-column index/value result into a one-column allocation. The worker stack reports `Index50000 out of bounds for length50000` in DenseBlockFP64.set / d_uarimax / RowAggTask. This is a CP aggregation-kernel defect, not evidence that FED/FOUT is an illegal planning state.
- **Root cause**: two exposed row-index outputs use correction NONE. Output width was adjusted to2 only inside the sequential fallback, after the multi-thread decision. The parallel path captured `n2=1` and allocated the undersized buffer. Both MAX_INDEX and MIN_INDEX kernels write two columns.
- **Minimal repair**: move the existing row-index output-width normalization before multi-thread eligibility and dimension capture, using the already computed aggregate type. Remove its duplicated sequential-only condition. Arithmetic, execution placement, privacy and supported candidate domain are unchanged; no single-thread fallback is introduced.
- **Regression RED**: frozen6c JAR with updated component tests:16 methods,14 PASS and the2 new dense two-output max/min methods reproduce ArrayIndexOutOfBounds. The524288x4 fixtures force8 threads, compare against1 thread, assert output shape and independently checked index/value samples. The first diagnostic compilation failed due to a test-only obsolete accessor; it is preserved separately and is not claimed as semantic RED.
- **Files**: `runtime/matrix/data/LibMatrixAgg.java`; `test/component/matrix/MatrixAggKernelTest.java`.
- **Evidence**: CONTROL/GMM_RUNTIME_FAILURE_20260906/{REPORT.md,receipt.json}; CONTROL/GMM_TOPK_RED_GREEN_20260906/frozen-6c-semantic-red.log.
- **Verification / remaining**: independent source review CLEAR; fresh so008 integrated regression **192/192 PASS**,17 suites, zero failures/errors/skips, Maven rc0. The updated kernel/test hashes match the pre-test manifest. Evidence: CONTROL/GMM_STEPLM_GNMF_FINAL_INTEGRATED_20260906/ and CONTROL/GMM_RUNTIME_FAILURE_20260906/receipt.json. Authenticated GMM runtime canary remains required after immutable deployment; the previous failed archive is never replaced. Fixing buffer allocation does not by itself prove later GMM numeric parity or planner runtime ordering.
- **Potential regression**: ordinary one-output row-index uses LASTCOLUMN correction and is outside the moved zero-correction normalization; unrelated aggregate types are excluded. Existing dense/sparse component aggregation tests and subsequent real GMM execution guard the shared-kernel change.

## Integrated ALS/P1 placement and native-result cost corrections

- **Status**: isolated regression and planning canaries passed; final combined regression and immutable deployment are separate gates. Runtime-ordering defects are not closed by these compiler tests.
- **Constraint**: every selector still consumes one shared privacy-filtered domain. No CP/FOUT transient or recompile exception, runtime fallback, protected raw collection, or candidate-derived proof was added.
- **ALS worker=1 symptom/root cause**: FULL native operations lost an otherwise exact single endpoint when a companion local/unknown input contaminated the endpoint join. Preserve an intrinsically known one-worker map where the runtime operation's conditional FULL contract permits a local companion; conflicting remote endpoints remain unknown. The binary oracle requires the structural one-partition proof. Files: `placement/SinglePartitionFacts.java`, `rules/Rulesets.java`. Actual ALS/L2SVM planning canary: 16/16 successful; all eight w3 plans unchanged. Tiny private-aggregate ALS runtime/recompile regression is also retained.
- **ALS latent WDivMM symptom/root cause**: source-level transpose/inner-multiply occurrences were not recognized as their surviving Pattern-1 WDivMM runtime owner after a conservative loop-shape join. Capture the immutable source-compiled shape separately from the conservative shape lattice and fingerprint it. Admit a runtime owner only when exact occurrence edges, compatible known dimensions, the single-consumer pattern and legal weights establish the substitution. Candidate alternatives and shared cost semantics use the same fact. The removed shell does not independently own the runtime instruction or its transfers. Files: `PlacementShapeFacts.java`, `PlacementAnalysis.java`, `NeutralPlacementGraphBuilder.java`, `PlacementCostSemantics.java`.
- **P1 transient symptom/root cause**: a legitimate exact reaching-definition edge was rejected solely because its write/read straddled compiled and dynamic-recompile physical contexts. Match the existing exact value/function/namespace identity across the two supported concrete modes; clone contexts, CP/FOUT transient states and missing anchors are still rejected. Four cross-recompile transient regressions preserve this boundary.
- **P1 function/RMEMPTY symptom/root cause**: Hop-only value relations do not contain all expanded function return/formal edges. Add structural occurrence endpoint closure after exact function/CFG/stable-origin edges exist, then replay candidate facts using exact physical input occurrence keys. RMEMPTY preserves its target's FederationMap, not the local/broadcast `select` operand's map; its endpoint dependency is target-only. The first actual unknown was RMEMPTY before `m_scale`, contaminating Xscaled/Xtrain and `e_step::X`. Final P1 canaries pass all four planners for both w1 and w3, with complete traces and no runtime execution. The closure never borrows selected candidates or worker-pool availability as cardinality evidence. Missing/foreign/conflicting endpoints and ungrounded cycles fail closed. Residual hardening watch: malformed RMEMPTY without the compiler-required target fails closed by exception rather than returning UNKNOWN.
- **Native LOUT cost symptom/root cause**: native aggregate-binary/aggregate-add FED/LOUT already returns its payload in the instruction request batch. Charging a second GET request RTT double-counted the round trip. Remove that extra RTT only on the proven native in-band path; preserve result payload, partial fan-in/reduction, separately materialized FOUT collection and unknown fallback costs. Seven focused fixed-instruction-stage tests and the existing fallback-integration suite guard this change. File: `fedCostBased/commons/FederatedCostModel.java`.
- **ALS test contract correction**: the former regression directly traversed the legacy memo-DP source subtree and demanded a self-cost ranking for a fused-away b(*) shell. Current `COMPILE_COST_BASED` selects `FederatedPlanLocalCost`/`LocalPhysicalOptimizer`. Replace that obsolete assertion tree with production-local and Exact optimization on one physical cost surface: identical cost fingerprint, Exact objective no worse than local, retained native FED/LOUT/runtime-FType state, and matching selected runtime owner/weights when the selected owner is FED. No model price or selected outcome was changed to satisfy an expected ranking. Two fixed-shape fixture lookups use the immutable source snapshot rather than requiring the conservative loop join to be exact.
- **Reproduction/evidence**: `/home/mchoi/g014-runtime-4net-w1357-20260901-control/INTEGRATED_CANDIDATE_20260906/INITIAL_PATCHES.json`, `REGRESSION_V1/`, `ALS_TEST_REPAIR_V3_PRODUCTION_LOCAL/`; `P1_C5_FAILURE_20260906/CANDIDATE_V4_RMEMPTY_FUNCTION_CARDINALITY/` (41 focused fact/guard tests and eight actual planning canaries); `ALS_LATENT_WDIVMM_SHAPE_20260906/CANDIDATE_V10/` (real owner, immutable-source and second-consumer hostile regressions).
- **Potential regressions/how detected**: endpoint over-propagation across incompatible callers (mixed-endpoint, unknown/cycle and multi-worker tests); broad shape-proof promotion (known-axis conflict, immutable source and second-consumer tests); double-counted or omitted runtime movement (shared physical factor and fixed-instruction-stage tests). Full repository test suite and fresh workload runtime remain distinct required evidence for any broader claims.

## P2 transformencode metadata privacy exclusion is not an endpoint bug

- **Status**: diagnosed; no privacy relaxation or fabricated successful runtime.
- **Observed**: all 64 historical c5 P2 cells stop at transformencode output position 1, `FunOut M`, line 11. The pre-privacy output domain is coordinator CP/LOUT; metadata inherits PRIVATE_AGGREGATE and cannot be exposed at the coordinator. All four selectors receive the same rejection before runtime instructions execute.
- **Runtime reason**: transformencode merges worker recode encoders into coordinator metadata containing observed category values. This is not an aggregate-only release. The current runtime does not supply a native remote metadata output alternative that would solve this origin-residency requirement. P1 endpoint/cardinality changes therefore cannot legalize P2. Existing dummycode/distinct-value privacy regression preserves non-release.
- **Evidence**: CONTROL/P2_PRIVACY_SCOPE_DIAGNOSIS_20260906/P2_PRIVACY_SCOPE_DIAGNOSIS.md and its exact-64 derived evidence. Successor orchestration retains these as explicitly historical scope exclusions, not fresh planning successes or results from the integrated backend.
- **Remaining**: a public, independently specified encoding dictionary or a different privacy-preserving transform protocol would be a separate workload/runtime design, not an optimizer guard relaxation. Do not silently relabel private-derived metadata as public, treat an old runtime as current privacy compliance, or plot a rejected P2 cell as a successful measurement.

## Integrated V2 candidate replay regression — BLOCK, diagnosis in parallel

- **Status**: not approved for deployment. Fresh 18-suite run on so008 contains 270 tests: 266 pass, 1 assertion failure, 3 errors, no skips. Evidence: CONTROL/INTEGRATED_CANDIDATE_20260906/REGRESSION_V2/summary.json and immutable source/after-build manifests.
- **Symptom**: `testDpDerivedFedFoutWhenOracleOnlyAllowsFedLout` loses the exact ROW aggregate domain's graph-owned derived-FOUT emission; `testDpPlansAlsWithUnrolledFunctionOutputCarrier` and `testDpLogRegKeepsInitialFederatedFormalForRowSumSquares` reject selected FULL consumers whose exact source is CP/LOUT; `testDpEnumerateFunctionPlaceholderIncludesMappedFunctionOutputs` reports AMBIGUOUS_TRANSIENT_FORWARD.
- **Current hypothesis**: P1 V4 seeds every original ordinal into the general post-CFG physical candidate replay after occurrence endpoint closure. This can rebuild unrelated input domains, anchors and derived actions although only single-endpoint evidence changed. A simple ROW aggregate changing is outside the intended proof refresh scope. Controlled no-replay and selective-replay comparisons are being run; the causal classification is not yet final.
- **Decision**: do not waive these failures because the production factory uses LocalPhysicalOptimizer rather than the directly invoked legacy DP API. The aggregate assertion explicitly inspects shared candidate facts. Do not remove tests, weaken reachability/privacy checks, or close supported runtime states to obtain green output.
- **Repair boundary**: retain exact occurrence/source identities and existing physical authority. A cardinality-only improvement must not erase unrelated legal candidates or derived materialization actions. Candidate-generated facts cannot prove cardinality. Any actual proof invalidation must be represented and independently tested, not ignored.
- **Validation before promotion**: four failing methods; full fallback integration; shared privacy/anchor/shape/cost suites; actual P1 four-planner compile-only canaries. Earlier P1 V4 canaries (8/8 for workers1 and3) do not substitute for a green integrated regression.
- **Residual risk**: selective change tracking can miss synthetic function/CFG propagation, while indiscriminate replay can drop authority. Function-return/loop/conflicting-endpoint negatives and the unchanged ROW aggregate are required controls.

## ALS native input cost unknown-sentinel reconciliation — verified in V2

- **Symptom**: an actual ALS line126 input has immutable source dimensions 50000x10 but a conservative loop fact (-1,10); bounded native-input costing returns absent instead of estimating the actual source input.
- **Repair**: use immutable source-compiled dimensions only when conservative dimensions are incomplete and every known conservative axis agrees. Known zero is a known axis (>=0), not an unknown sentinel; zero-sized or conflicting inputs remain rejected. No candidate legality, geometry, privacy or runtime rule changes.
- **Files**: placement/PlacementCostSemantics.java; PlacementCostSemanticsNativeInputSourceShapeTest.java. The obsolete fused-away memo self-cost fixture was separately replaced with assertions over the production local optimizer and the identical Exact cost surface, not runtime ordering.
- **Evidence**: CONTROL/ALS_NATIVE_INPUT_COST_SENTINEL_20260906/CANDIDATE_V1/candidate.patch (SHA256 6d83cd7f38715b75e1c14b83601e3459d7f1158e02aba79259f4b53c6ddfe4d4). Genuine pre-fix ALS regression RED; focused six methods GREEN; integrated V2 new five-test class and all11 ALS cost tests pass.
- **Remaining/risk**: runtime impact awaits an approved backend and physical-plan-only comparison. Do not treat cost-only source facts as privacy, partition, or movement feasibility evidence; zero/conflict tests guard this boundary.

## P1 cardinality replay isolation — causal repair and non-emitted-function regression guards

- **Cause confirmed**: changing only V4's all-original producer replay seed to an empty list makes the same four R2 failures pass against frozen R2 classes. Empty replay is only a diagnostic; it would omit necessary P1 proof updates. Evidence: CONTROL/FALLBACK_CARDINALITY_REPLAY_COUNTERFACTUAL_20260906/REPORT.md.
- **Production repair**: seed the existing physical-consumer worklist only for original occurrence inputs whose candidate-facing single-partition evidence changes (unknown versus known). Compare knownness, not endpoint text: the oracle consumes a cardinality Boolean; endpoint alignment remains independent. The closure still derives evidence solely from program structure, never candidate/selected-plan state.
- **Safety boundary**: both proof-change directions are detected. Unsupported contradictory old-known/new-unknown evidence must still fail closed under existing physical-refinement checks; no new blanket permission to remove FULL rows was introduced. Normal function-input/output ambiguity is UNKNOWN before occurrence closure. The change does not claim that every future weakening topology is supported.
- **V6 verification**: five focused delta tests and the original four failed methods pass. Fresh integrated R3: 275 tests,274 pass,one fixture assertion failure (not a planner exception). All130 existing fallback tests pass.
- **Fixture diagnosis**: the new conflicting-caller `prune` function was a single-block inlineable function. The graph correctly has two emitted main RMEMPTY operations and a FUNCTION_BODY_NON_EMITTED original body. V4 blanket replay incorrectly resurrected that dead body through buildNode; V6 preserves its exclusion. A test requiring this dead body to be an active decision was invalid.
- **Fixture repair**: use a genuine runtime-branch function and require one active shared RMEMPTY with no FULL authority across conflicting caller endpoints. Preserve the simple inlineable script as two additional hostiles (same and different endpoint callers): the body must remain non-emitted/legal-empty, while both real inlined main operations must remain active FULL/FOUT. No assertion was disabled or weakened to accept an invalid selected plan.
- **Files**: placement/SinglePartitionFacts.java; NeutralPlacementGraphBuilder.java; SinglePartitionFactsTest.java; FunctionReturnSinglePartitionFactsTest.java.
- **Evidence**: CONTROL/P1_C5_FAILURE_20260906/CANDIDATE_V6_CARDINALITY_EVIDENCE_DELTA/ (four integration +five delta tests), CANDIDATE_V7_FIXTURE_CORRECTION/ (9/9 function-return tests). Final root full-suite evidence is recorded separately under CONTROL/INTEGRATED_CANDIDATE_20260906/REGRESSION_V4; real P1 compile-only canary status is a separate gate.
- **Remaining/risk**: runtime performance and all-workload legality are not proven by unit tests. Preserve exact source/JAR/stage identities, authenticate fresh planning against old runtime authority, and rerun only physically changed eligible cells. No runtime fallback, privacy weakening, or CP/FOUT transient/recompile exception was added.
